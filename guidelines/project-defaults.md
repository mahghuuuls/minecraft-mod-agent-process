## Java Compatibility

- Use Java 25 to run Gradle and the development environment.
- Release artifacts must remain compatible with Java 8.
- Modern Java syntax may be used only through the support provided by the project template.
- Do not use Java APIs introduced after Java 8 unless they are provided through an explicitly approved compatible dependency.